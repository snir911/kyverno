apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: add-kata-runtimeclass
spec:
  rules:
  - name: "Set RuntimeClass to kata"
    match:
      resources:
        kinds:
        - Pod
    mutate:
      overlay:
        spec:
         runtimeClassName: kata
